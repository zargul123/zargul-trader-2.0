The Core Issue
Your trading model is designed as a sequence-to-sequence predictor that outputs:

1 price prediction per timestep

For 3 different time horizons (e.g., 1hr, 4hr, 24hr)

This creates 3 outputs per input sequence, causing the 516:172 ratio (516 ÷ 172 = 3).

Complete Solution
python
# --- UPDATE YOUR PREDICTION FUNCTION ---

def pca_predict(x_pca):
    # 1. Inverse PCA and reshape
    x_flat = pca.inverse_transform(x_pca)
    x_reshaped = x_flat.reshape(x_flat.shape[0], sequence_length, len(features))
    
    # 2. Get model predictions
    preds = model.predict(x_reshaped, verbose=0)
    
    # 3. Handle different output types
    if isinstance(preds, tuple):
        # For models returning (output, states)
        final_preds = preds[0]
    elif isinstance(preds, list):
        # For multi-output models
        final_preds = preds[0]  # Primary output
    else:
        final_preds = preds
    
    # 4. CRITICAL: Select only the primary horizon prediction
    print(f"Raw model output shape: {final_preds.shape}")  # Debug
    
    # Case: 3D output (samples, timesteps, horizons)
    if len(final_preds.shape) == 3:
        # Use only the last timestep and primary horizon
        return final_preds[:, -1, 0].flatten()
    
    # Case: 2D output (samples, horizons)
    elif len(final_preds.shape) == 2:
        # Use only primary horizon
        return final_preds[:, 0].flatten()
    
    return final_preds.flatten()
Key Changes:
Horizon Selection:

python
return final_preds[:, -1, 0].flatten()
-1: Last timestep (most recent prediction)

0: Primary prediction horizon (e.g., 1hr)

Debug Output:

python
print(f"Raw model output shape: {final_preds.shape}")
Helps verify your model's output structure

Verification Step (Add Before SHAP):
python
# Test prediction output
test_output = pca_predict(test_pca[:1])
print(f"Input samples: {test_pca[:1].shape}")
print(f"Processed output shape: {test_output.shape}")